[project]
name = "shop-assistant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Abduraufdev0788", email = "turkeynumber063@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "anthropic>=1.6.0",
    "numpy>=2.5.3",
    "pytest>=9.1.1",
    "python-dotenv>=1.2.3",
    "telethon>=1.45.0",
    "voyageai>=0.5.0",
]

[project.scripts]
shop-assistant = "shop_assistant:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
